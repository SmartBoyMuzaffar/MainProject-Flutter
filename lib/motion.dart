import 'package:flutter/material.dart';

class Motion extends StatelessWidget {
  const Motion({super.key});

  static const routeName = '/motion';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Motion Control"),
      ),
      body: const Center(
        child: Text('Coming Soon...!'),
      ),
    );
  }
}

